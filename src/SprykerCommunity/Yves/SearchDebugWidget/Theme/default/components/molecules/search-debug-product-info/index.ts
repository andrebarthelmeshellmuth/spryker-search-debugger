// The matched-token badges are styled by the sibling component's stylesheet (the single source of the
// token palette) — imported here so this component's styling does not silently depend on the headline
// component happening to be bundled as well.
import '../search-debug-tokens/search-debug-tokens.scss';
import './search-debug-product-info.scss';
